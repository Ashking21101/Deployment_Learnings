create instant.py with fastapi write code in it(making apenai app)

add fastapi and uvicorn in requirements.txt

in terminal write = vercel env add OPENAI_API_KEY and enter your key to add it to vercel(for deployment)

create a vercel.json and add code

then install node and npm(if not already)

then in terminal = npm install -g vercel (meaning install vercel command line tool)

then in terminal = vercel login

to deploy write in terminal = vercel . (For preview)
                              vercel dev (for developement)
                              vercel --prod (For Production)

and go to vercel webste and to your project and disable its protection

